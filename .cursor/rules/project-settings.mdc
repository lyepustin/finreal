---
description: 
globs: 
---
---
description: env
globs: 
---
always return svelte5 commands
always run commands with pnpm for windows powershell
database should be supabase
app focused to be used through the browser of a mobile phone (css)
components to use from shadcn-svelte
there is no need to check in every route the autentication because we already doing it in /src/routes/+layout.svelte
you can use any or none like a type for typescript
The `$state` rune is only available inside `.svelte` and `.svelte.js/ts` files